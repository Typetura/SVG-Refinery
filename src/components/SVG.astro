---
const filters = await Astro.glob('../filters/*.astro');
---

<svg id="refinery" width="0" height="0">

  <style is:global>
    :where(#refinery) {
      --light: yellow;
      --dark: purple;
    }
  </style>

  <defs>
    {filters.map((filter) => (
      <filter.default size={24} />
    ))}
  </defs>
</svg>
